# One copy of Angular for the whole workspace. Nested copies break InjectionToken
# identity: the builder's TestBed and the renderer's ConfigSourceService would
# otherwise load two @angular/core instances and inject() would throw NG0203.
install-strategy=hoisted
